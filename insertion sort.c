//Write a program for insertion sort//
#include<stdio.h>
int main()
{
	int i,j,a[i], 
	printf("Enter any 5 number for comparing");
	for(i=0;i<5;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=1;i<n;i++)
		{
			temp=a[i];
			j=i-1;
		while (j>=0&&a[j]>temp)
			{
				a[j+1]=a[j];
				j=j-1;
			}	
		}
	}
